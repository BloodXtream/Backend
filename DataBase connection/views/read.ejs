<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <script src="https://cdn.tailwindcss.com"></script>

</head>
<body>
    <div class="w-full h-full bg-zinc-800 p-2 text-white">
        <a class="mb-2 text-blue-500" href="/">Back to Home</a>
        <h1 class="text-[22px] tracking-tighter mb-1.5">All Users</h1>
        <div class="users flex flex-wrap gap-1">
            <% if(users.length > 0 ){  %>
                <%  users.forEach((user)=>{  %>
                    <div class="user w-52 rounded-lg p-1.5 bg-zinc-700">
                        <div class="w-full h-[250px] rounded-lg bg-zinc-500 overflow-hidden">
                            <img class="w-full h-full object-cover" src="<%= user.image %>" alt="">
                        </div>
                        <h3 class="text-[17px] mt-1 tracking-tighter"><%= user.name %></h3>
                        <h5 class="text-zinc-500 text-[10px]"><%= user.email  %></h5>
                        <div class="flex gap-1 mt-5">
                            <a class="text-sm text-blue-500 " href="">Edit User</a>
                            <a class="text-sm text-red-500" href="/delete/<%= user._id %>">Delete User</a>
                        </div>
                    </div>
                <%  })  %>
            <% } else{ %>
                <h3 class="text-red-500 ">NO USERS YET...!</h3>
            <% } %>
        </div>
       
    </div>
</body>
</html>